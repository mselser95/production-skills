# prod-incident configuration — copy to `config.sh` and edit. Live file is plain shell.
# Repo-relative directory for regression fixtures.
PROD_REGRESSIONS_DIR="regressions"
# Where candidate-invariant packages are deposited for ratification.
PROD_RATIFY_QUEUE_DIR=".prod/ratify-queue"
