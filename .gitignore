config.sh
domain-topology.yaml
.skill-optimizer/runs/
benchmark-results/
node_modules/
.DS_Store

# transient worktrees (one already leaked in as a gitlink once)
.wt/

# Local runs on a dirty tree produce records that are explicitly NOT attestations
# (tree_clean:false). Only the clean-tree <sha>.json records CI writes belong in git.
.prod/evidence/dirty-*.json
